[package]
name = "pairing"
version = "0.1.0"
edition = "2021"

[lib]
name = "pairing"
path = "src/lib.rs"

[dependencies]
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1"
