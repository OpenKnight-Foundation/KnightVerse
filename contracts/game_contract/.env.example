# Stellar Secret Key for contract deployment
# ⚠️  NEVER commit the actual .env file to version control!
#     The .env file is already gitignored by the root .gitignore.
#
# Generate or retrieve your Stellar testnet secret key and set it here:
STELLAR_SECRET_KEY=your_stellar_secret_key_here

